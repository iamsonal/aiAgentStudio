---
title: Security
description: Governance controls, trust layers, and production hardening guidance.
---

import { Aside, Card, CardGrid, Steps, TabItem, Tabs } from '@astrojs/starlight/components';

# Security

AI Agent Studio is designed so security and governance live inside the runtime model, not only in
surrounding process documentation.

## Core Security Layers

<CardGrid>
  <Card title="Salesforce security model" icon="seti:lock">
    User context, sharing, CRUD, and field-level access remain the first boundary for all framework
    behavior.
  </Card>
  <Card title="Trust layers" icon="seti:shield">
    Masking, prompt safety, tool gating, and approval controls reduce risk before an action is
    executed or a prompt leaves Salesforce.
  </Card>
  <Card title="Operational auditability" icon="seti:graph-line">
    Executions and execution steps are persisted so teams can review what happened after the fact.
  </Card>
  <Card title="Controlled side effects" icon="approve-check">
    Human-in-the-loop settings let teams keep sensitive changes under explicit review.
  </Card>
</CardGrid>

## Platform-Native Enforcement

### Data access

- queries should respect access through `WITH USER_MODE` or equivalent user-mode patterns
- DML should enforce field accessibility and run in user mode where intended
- the framework does not depend on privilege escalation to make normal agent behavior work

```apex
List<Account> accounts = [
    SELECT Id, Name
    FROM Account
    WHERE Id IN :accountIds
    WITH USER_MODE
];
```

```apex
SObjectAccessDecision decision = Security.stripInaccessible(
    AccessType.UPDATABLE,
    recordsToSave
);
Database.update(decision.getRecords(), false, AccessLevel.USER_MODE);
```

### Service-user routing

<Aside type="caution" title="Use sparingly">
Service-user routing should be used only when the workflow genuinely requires a separate execution
user context. Keep the service user tightly permissioned and operationally monitored.
</Aside>

## Trust Layers

<Tabs>
  <TabItem label="PII Masking">

Masking reduces the chance that raw sensitive values leave Salesforce.

<ul>
  <li>schema-aware and pattern-aware masking can be applied before provider calls</li>
  <li>masking behavior is configured per agent through <code>PIIMaskingPreset__c</code></li>
  <li>masking is especially important when prompts include record content or user-entered data</li>
</ul>

  </TabItem>
  <TabItem label="Prompt Safety">

Provider adapters can run safety behavior before or during model interaction.

<ul>
  <li>controlled through <code>PromptSafetyPreset__c</code></li>
  <li>implemented in the provider adapter layer</li>
  <li>useful when the org wants a provider-native moderation step before full execution</li>
</ul>

  </TabItem>
  <TabItem label="Tool Flow Graph">

The Tool Flow Graph limits which tools the model can currently see.

<ul>
  <li>entry tools are initially eligible</li>
  <li>downstream tools unlock only after valid prior steps</li>
  <li>exclusive branches can permanently lock sibling choices for the current execution</li>
</ul>

  </TabItem>
</Tabs>

## Human-in-the-Loop Modes

| Mode | What it does | Best fit |
| :-- | :-- | :-- |
| `Confirmation` | The user confirms directly in the conversation | lower-risk conversational writes |
| `Approval` | The runtime pauses for a formal approval step | higher-risk or external side effects |
| `ConfirmationThenApproval` | Requires chat confirmation before formal approval | conversational agents with elevated risk |
| `ConditionalApproval` | The model indicates whether approval is needed at runtime | cases where risk depends on the proposed action |

<Aside type="note" title="Mode constraints">
`ConfirmationThenApproval` is only valid for interactive conversational agents and should not be
combined with asynchronous tool execution.
</Aside>

## What the Runtime Records

Captured audit data can include:

- user messages
- assistant responses
- tool calls and tool results
- failure details and status transitions
- token counts and estimated cost
- tool rationale when enabled

This makes the framework easier to operate in production because the execution path is inspectable.

## Production Hardening Checklist

<Steps>
1. **Start in sandbox**

   Validate realistic users, data visibility, approvals, and provider behavior before rollout.

2. **Use least privilege**

   Keep users, service users, and approvers narrowly permissioned.

3. **Protect side effects**

   Put confirmation or approval in front of record updates, email sends, external callouts, and
   other impactful actions.

4. **Turn on trust layers deliberately**

   Add masking, safety, and tool gating based on the workflow risk instead of enabling everything
   blindly.

5. **Watch execution data**

   Monitor failure rates, token spikes, repeated approval rejections, and unusual routing behavior.
</Steps>
