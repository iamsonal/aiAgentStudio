<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>NextRetryAt__c</fullName>
    <description>Earliest time when this execution can be retried. Used for exponential backoff - workers will not pick up executions where NextRetryAt is in the future. Set when retrying a failed execution.</description>
    <inlineHelpText>Earliest retry time (for exponential backoff).</inlineHelpText>
    <label>Next Retry At</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>DateTime</type>
</CustomField>
