<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ProcessingStartedAt__c</fullName>
    <description>Timestamp when a worker began processing this execution. Used for stale job detection - if ProcessingStartedAt is set but execution is still in Processing status after a threshold, the job may be stuck.</description>
    <inlineHelpText>When a worker started processing this execution.</inlineHelpText>
    <label>Processing Started At</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>DateTime</type>
</CustomField>
