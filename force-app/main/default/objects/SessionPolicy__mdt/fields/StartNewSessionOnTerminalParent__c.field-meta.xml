<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>StartNewSessionOnTerminalParent__c</fullName>
    <defaultValue>false</defaultValue>
    <description>When enabled, the runtime starts a new session if the parent record is already in a terminal state defined by this policy.</description>
    <fieldManageability>DeveloperControlled</fieldManageability>
    <inlineHelpText>Enable when a closed or terminal parent record should force the next inbound message into a fresh session instead of resuming the old one.</inlineHelpText>
    <label>Start New Session On Terminal Parent</label>
    <type>Checkbox</type>
</CustomField>
