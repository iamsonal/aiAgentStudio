<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>IsActive__c</fullName>
    <defaultValue>true</defaultValue>
    <description>Whether this action handler is active and available for use in capability configurations. Inactive handlers are hidden from the configurator UI and cannot be selected for new capabilities.</description>
    <fieldManageability>DeveloperControlled</fieldManageability>
    <inlineHelpText>Inactive handlers are hidden in the UI and cannot be assigned to new capabilities. Existing capabilities referencing an inactive handler will fail at runtime. Deactivate instead of deleting.</inlineHelpText>
    <label>Active</label>
    <type>Checkbox</type>
</CustomField>
